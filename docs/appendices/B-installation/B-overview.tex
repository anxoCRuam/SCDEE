Este anexo contiene la receta paso a paso para levantar el
\dfn{backend} del SCDEE desde un repositorio limpio. El sistema completo
se orquesta mediante \texttt{docker compose}, y todas las operaciones
habituales están encapsuladas en el \texttt{Makefile} raíz del proyecto.
