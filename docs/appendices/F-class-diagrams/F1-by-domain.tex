Esta sección presenta los nueve diagramas de clase restantes del
modelo de datos, agrupados por subdominio en el mismo orden seguido
en \cref{SEC:DES-DATA}.

\begin{figure}[Diagrama de clase del subdominio \texttt{accounts}]%
              {fig:class-accounts}%
              {Diagrama de clase del subdominio \texttt{accounts}
               (usuarios, autenticación, lista negra de \acsp{jwt}).}
  \image{}{}{graph_models/accounts_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{organizations}]%
              {fig:class-organizations}%
              {Diagrama de clase del subdominio \texttt{organizations}
               (organización inquilino y configuración).}
  \image{}{}{graph_models/organizations_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{courses}]%
              {fig:class-courses}%
              {Diagrama de clase del subdominio \texttt{courses}
               (cursos académicos y transiciones).}
  \image{}{}{graph_models/courses_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{subjects}]%
              {fig:class-subjects}%
              {Diagrama de clase del subdominio \texttt{subjects}
               (asignaturas, grupos y membresías).}
  \image{}{}{graph_models/subjects_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{annotations}]%
              {fig:class-annotations}%
              {Diagrama de clase del subdominio \texttt{annotations}
               (anotaciones de texto, trazos a lápiz y audio).}
  \image{}{}{graph_models/annotations_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{grading}]%
              {fig:class-grading}%
              {Diagrama de clase del subdominio \texttt{grading}
               (calificaciones, reglas de asignación y rúbricas).}
  \image{}{}{graph_models/grading_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{reviews}]%
              {fig:class-reviews}%
              {Diagrama de clase del subdominio \texttt{reviews}
               (ventanas de revisión y solicitudes).}
  \image{}{}{graph_models/reviews_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{notifications}]%
              {fig:class-notifications}%
              {Diagrama de clase del subdominio \texttt{notifications}
               (notificaciones \textit{in-app} y plantillas de correo).}
  \image{}{}{graph_models/notifications_class}
\end{figure}

\begin{figure}[Diagrama de clase del subdominio \texttt{audit}]%
              {fig:class-audit}%
              {Diagrama de clase del subdominio \texttt{audit}
               (registro de auditoría inmutable).}
  \image{}{}{graph_models/audit_class}
\end{figure}
